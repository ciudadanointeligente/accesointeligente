<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
		"-//Hibernate/Hibernate Configuration DTD 3.0//EN"
		"http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
	<session-factory>

		<!-- Database connection settings -->
		<property name="connection.driver_class">com.mysql.jdbc.Driver</property>
		<property name="connection.url">jdbc:mysql://localhost/accesointeligente</property>
		<property name="connection.username">accesoint</property>
		<property name="connection.password">accesointeligente</property>

		<!-- JDBC connection pool (use the built-in) -->
		<property name="connection.pool_size">1</property>

		<!-- SQL dialect -->
		<property name="dialect">org.hibernate.dialect.MySQL5InnoDBDialect</property>

		<property name="transaction.factory_class">org.hibernate.transaction.JDBCTransactionFactory</property>

		<!-- Enable Hibernate's automatic session context management -->
<!--		TODO: Hibernate offers three methods of current session tracking. -->
<!--		The "thread" based method is not intended for production use; it is merely useful for prototyping and tutorials such as this one. -->
		<property name="current_session_context_class">thread</property>

		<!-- Disable the second-level cache  -->
		<property name="cache.provider_class">org.hibernate.cache.NoCacheProvider</property>

		<!-- Echo all executed SQL to stdout -->
		<property name="show_sql">false</property>

		<!-- Drop and re-create the database schema on startup -->
		<property name="hbm2ddl.auto">update</property>
		
		<mapping resource="cl/votainteligente/accesointeligente/model/Activity.hbm.xml"/>
		<mapping resource="cl/votainteligente/accesointeligente/model/Age.hbm.xml"/>
		<mapping resource="cl/votainteligente/accesointeligente/model/InstitutionType.hbm.xml"/>
		<mapping resource="cl/votainteligente/accesointeligente/model/Region.hbm.xml"/>
		<mapping resource="cl/votainteligente/accesointeligente/model/User.hbm.xml"/>
	</session-factory>
</hibernate-configuration>